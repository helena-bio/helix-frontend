/**
 * Settings Page
 *
 * Full-page settings view with left navigation and right content.
 * Replaces the old modal-based settings.
 */
'use client'

import { SettingsView } from '@/components/settings/SettingsView'

export default function SettingsPage() {
  return <SettingsView />
}
